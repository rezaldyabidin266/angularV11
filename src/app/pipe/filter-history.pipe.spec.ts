import { FilterHistoryPipe } from './filter-history.pipe';

describe('FilterHistoryPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterHistoryPipe();
    expect(pipe).toBeTruthy();
  });
});
