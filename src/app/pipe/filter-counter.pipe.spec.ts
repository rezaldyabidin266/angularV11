import { FilterCounterPipe } from './filter-counter.pipe';

describe('FilterCounterPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCounterPipe();
    expect(pipe).toBeTruthy();
  });
});
