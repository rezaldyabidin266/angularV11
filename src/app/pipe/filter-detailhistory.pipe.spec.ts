import { FilterDetailhistoryPipe } from './filter-detailhistory.pipe';

describe('FilterDetailhistoryPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDetailhistoryPipe();
    expect(pipe).toBeTruthy();
  });
});
